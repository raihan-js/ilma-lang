name = "ilma-tutor"
main = "src/index.js"
compatibility_date = "2024-09-01"
compatibility_flags = ["nodejs_compat"]

[ai]
binding = "AI"

# Free plan limits: 100,000 requests/day, 10ms CPU/request
# Workers AI free neurons: 10,000/day (enough for ~200 student sessions)
